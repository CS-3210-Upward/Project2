from pdb import line_prefix
from turtle import update


class LexicalAnalyzer:
    def __init__(self, file):
        self.file = file
    
    def check_indentation(file):
        with open file('file.txt.', 'r')
        for line in file:
            if line != '\t':
                line = '\t'
                return line
        with open file('updated_file.txt', 'w')
        return 'updated_text.txt'
    
    def verify_function_headers(file):
        with open file('file.txt', w) as f:
            for line in lines:
                if lines.startswith('def'):
                    return line
                else:
                    f.write('def')
        return line
    
    def count_print(file):
        with open('file.txt', r):
            count = 0
            for line in file:
                if line.contains('print)'):
                    count += 1
        return count
    
    def output_to_file(file):
        with open file('file.txt', w) as f:
            for line in f:
                f.write(line) #did not know which to use
            updated_file = f.
        return('file.txt')  